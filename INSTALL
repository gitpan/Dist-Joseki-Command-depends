This is the Perl distribution Dist-Joseki-Command-depends.

## Installation

Dist-Joseki-Command-depends installation is straightforward. If your CPAN shell
is set up, you should just be able to do

    % cpan Dist::Joseki::Command::depends

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

## Documentation

Dist-Joseki-Command-depends documentation is available as in POD. So you can
do:

    % perldoc Dist::Joseki::Command::depends

to read the documentation online with your favorite pager.

Marcel Gruenauer
